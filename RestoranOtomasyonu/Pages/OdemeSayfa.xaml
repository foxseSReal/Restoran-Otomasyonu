<Page x:Class="RestoranOtomasyonu.Pages.OdemeSayfa"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RestoranOtomasyonu.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="824" d:DesignWidth="935"
      Title="OdemeSayfa">
    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                     From="0" To="1" Duration="0:0:0.3" />
                    <ThicknessAnimation Storyboard.TargetProperty="Margin" 
                        From="0,0,0,800" To="0,0,0,0" Duration="0:0:1" 
                        DecelerationRatio="0.9" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Page.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="250"/>
            <RowDefinition Height="574"/>
        </Grid.RowDefinitions>

        <!--Odeme Geçmişi ve Kısa yollar-->
        <!--row0-->
        <Grid Grid.Row="0">

        </Grid>
        <!--row1-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <!--Odeme Kisayol Butonlari-->
            <Border Padding="0 20 0 20">
                <StackPanel Grid.Column="0">
                    <Button x:Name="NakitBnt" Style="{StaticResource OdemeKisayolBtn}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Nakit" FontSize="28" VerticalAlignment="Center"/>
                            <materialDesign:PackIcon Kind="Cash" Margin="20 0 0 0" Width="50" Height="50"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="KartBnt" Style="{StaticResource OdemeKisayolBtn}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Kart" FontSize="28" VerticalAlignment="Center"/>
                            <materialDesign:PackIcon Kind="CreditCardFast" Margin="32 0 0 0" Width="50" Height="50"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="CariBnt" Style="{StaticResource OdemeKisayolBtn}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Cari" FontSize="28" VerticalAlignment="Center"/>
                            <materialDesign:PackIcon Kind="NoteMinus" Margin="40 0 0 0" Width="50" Height="50"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="VeresiyeBnt" Style="{StaticResource OdemeKisayolBtn}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Veresiye" FontSize="28" VerticalAlignment="Center"/>
                            <materialDesign:PackIcon Kind="CashMinus" Margin="10 0 0 0" Width="50" Height="50"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
